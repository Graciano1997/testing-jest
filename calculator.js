const calculator = {
    add: (a, b) => parseInt(a) + parseInt(b),
    subtract: (a, b) => parseInt(a) - parseInt(b),
    multitply: (a, b) => a * b,
    divide: (a, b) => a / b,
}

module.exports = calculator;